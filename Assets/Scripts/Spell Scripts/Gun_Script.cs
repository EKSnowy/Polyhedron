using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class Gun_Script : MonoBehaviour
{
    /*public IceShard_Script iceShard;
    public Vector2 direction;

    public void Update()
    {
        direction = (transform.localRotation * Vector2.up).normalized;
    }

    public void Shoot()
    {
        GameObject obj = Instantiate(iceShard.gameObject, transform.position, Quaternion.identity);
        IceShard_Script shard = obj.GetComponent<IceShard_Script>();
        shard.direction = direction;
    }

    public Vector2 getDirection()
    {
        return direction;
    }*/

    
}
